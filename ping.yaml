- name : ping the server
  hosts: frontend
  tasks:
    - name: ping the server
      ansible.builtin.ping:
      register: result

    - name: show ping result
      ansible.builtin.debug:
        var: result